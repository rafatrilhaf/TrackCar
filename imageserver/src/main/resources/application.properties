# -------------------------------------------------------------------
# Configuração da aplicação
# -------------------------------------------------------------------
spring.application.name=imageserver
server.port=8080

# -------------------------------------------------------------------
# MongoDB Atlas (GridFS) – substitua usuário e senha
# -------------------------------------------------------------------
spring.data.mongodb.uri=mongodb+srv://joaojotarc02_db_user:joaopedrorc@clustertc.66e8ozq.mongodb.net/trackcardb?retryWrites=true&w=majority&tls=true
spring.data.mongodb.database=trackcardb

# -------------------------------------------------------------------
# Multipart upload
# -------------------------------------------------------------------
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# -------------------------------------------------------------------
# CORS (caso ainda não use CorsConfig)
# -------------------------------------------------------------------
# Se tiver CorsConfig, pode remover estas três linhas
# Uncomment abaixo apenas se necessário
# cors.allowed.origins=*
# cors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS
# cors.allowed.headers=*

# -------------------------------------------------------------------
# Logging para debug do MongoDB
# -------------------------------------------------------------------
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.mongodb.driver=INFO
